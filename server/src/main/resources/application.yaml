spring:
  datasource:
    url: jdbc:mysql://localhost:3306/articket
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  jackson:
    property-naming-strategy: LOWER_CAMEL_CASE

  profiles:
    include:
      secret
